/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'wembleypark.com', 'www.andbeyond.com', 'www.stlmag.com']
  }
}

module.exports = nextConfig
